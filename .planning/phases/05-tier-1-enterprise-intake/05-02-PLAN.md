---
phase: 05-tier-1-enterprise-intake
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/company/profile/page.tsx
  - app/components/tier1/CorporateIdentityTab.tsx
  - app/components/tier1/VehiclesCertificationsTab.tsx
  - app/components/tier1/CapabilitiesPositioningTab.tsx
  - app/components/tier1/Tier1TabLayout.tsx
autonomous: true
requirements:
  - TIER1-01
  - TIER1-03
  - TIER1-04
  - TIER1-06

must_haves:
  truths:
    - "User sees a 3-tab interface (Corporate Identity, Vehicles & Certifications, Capabilities & Positioning) when navigating to the company profile page"
    - "User can enter and save all corporate identity fields (legal entity name, UEI, CAGE, primary NAICS, business size, socioeconomic certifications) in the first tab"
    - "User can enter and save contract vehicles, ISO/CMMI/ITIL status, facility clearance level, and DCAA-approved system status in the second tab"
    - "User can enter and save corporate overview, core services, enterprise win themes, key differentiators, and standard management approach in the third tab"
    - "Each tab has its own Save button that persists only that tab's data"
    - "Switching tabs does not lose unsaved changes on the current tab"
    - "UEI and NAICS validation errors display inline before save"
    - "Word count indicators display on capability text fields"
  artifacts:
    - path: "app/company/profile/page.tsx"
      provides: "Tier 1 tabbed enterprise intake page (replaces existing profile page)"
      min_lines: 50
    - path: "app/components/tier1/Tier1TabLayout.tsx"
      provides: "Tab navigation wrapper component with tab state management"
      exports: ["Tier1TabLayout"]
    - path: "app/components/tier1/CorporateIdentityTab.tsx"
      provides: "Corporate Identity form tab with UEI/NAICS validation"
      exports: ["CorporateIdentityTab"]
    - path: "app/components/tier1/VehiclesCertificationsTab.tsx"
      provides: "Vehicles & Certifications form tab with ISO/CMMI/DCAA fields"
      exports: ["VehiclesCertificationsTab"]
    - path: "app/components/tier1/CapabilitiesPositioningTab.tsx"
      provides: "Capabilities & Positioning form tab with word count enforcement"
      exports: ["CapabilitiesPositioningTab"]
  key_links:
    - from: "app/company/profile/page.tsx"
      to: "app/components/tier1/Tier1TabLayout.tsx"
      via: "renders Tier1TabLayout with company data"
      pattern: "Tier1TabLayout"
    - from: "app/components/tier1/CorporateIdentityTab.tsx"
      to: "/api/company/profile"
      via: "fetchWithCompany PUT on save"
      pattern: "fetchWithCompany.*PUT"
    - from: "app/components/tier1/CorporateIdentityTab.tsx"
      to: "lib/validation/tier1-validators.ts"
      via: "inline validation on UEI/CAGE/NAICS fields"
      pattern: "validateUEI|validateNAICS|validateCAGE"
    - from: "app/components/tier1/CapabilitiesPositioningTab.tsx"
      to: "lib/validation/tier1-validators.ts"
      via: "word count validation on text fields"
      pattern: "validateWordCount"
---

<objective>
Build the 3-tab Tier 1 Enterprise Intake UI that replaces the existing company profile page.

Purpose: This is the primary user-facing deliverable of Phase 5. The tabbed form captures all stable corporate data defined in Blueprint section 4.1 across three organized tabs, with per-tab save behavior and inline validation. The form replaces the existing company profile page entirely per user decision.

Output: Tabbed intake form with Corporate Identity, Vehicles & Certifications, and Capabilities & Positioning tabs.
</objective>

<execution_context>
@C:/Users/eharr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eharr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tier-1-enterprise-intake/05-CONTEXT.md
@.planning/phases/05-tier-1-enterprise-intake/05-01-SUMMARY.md
@lib/supabase/company-types.ts
@lib/validation/tier1-validators.ts
@app/api/company/profile/route.ts
@app/api/company/certifications/route.ts
@app/api/company/capabilities/route.ts
@app/company/profile/page.tsx
@lib/context/CompanyContext.tsx
@lib/hooks/useFetchWithCompany.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tab layout wrapper and Corporate Identity tab</name>
  <files>
    app/components/tier1/Tier1TabLayout.tsx
    app/components/tier1/CorporateIdentityTab.tsx
    app/company/profile/page.tsx
  </files>
  <action>
    **Tier1TabLayout.tsx** — Create a reusable tab layout component:
    - 'use client' component
    - Props: `activeTab: string`, `onTabChange: (tab: string) => void`, `children: React.ReactNode`, `tabs: {id: string, label: string, icon?: React.ReactNode}[]`
    - Render horizontal tab bar with 3 tabs: "Corporate Identity", "Vehicles & Certifications", "Capabilities & Positioning"
    - Active tab highlighted with blue bottom border (Tailwind: `border-b-2 border-blue-600 text-blue-600`)
    - Inactive tabs: `text-gray-500 hover:text-gray-700`
    - Tab bar is sticky at top of form area
    - Renders `children` below the tab bar (children will be the active tab's form)
    - Use Tailwind classes matching existing codebase style (white bg, shadow-md, rounded-lg cards)

    **CorporateIdentityTab.tsx** — Create the first tab form:
    - 'use client' component
    - Props: `companyId: string`, `initialData: Partial<CompanyProfile>`, `onSaved: () => void`
    - Local state for all Corporate Identity fields using useState (not controlled by parent — per user decision, each tab manages its own form state)
    - Fields (organized into card sections):
      **Company Identity section:**
      - Legal Entity Name (text, required) — maps to `legal_name`
      - Company Name / DBA (text, required) — maps to `company_name`
      - DBA Names (tag input, add/remove pattern from existing page)
      - Logo URL (url input with preview, from existing page)

      **Government Registration section:**
      - UEI Number (text, required, maxLength 12) — maps to `uei_number`
        - Inline validation on blur using `validateUEI()` from `@/lib/validation/tier1-validators`
        - Show validation error below field in red text
        - After valid UEI: trigger `checkSAMStatus()` and display result badge (Active=green, Expired=yellow, Not Found=red)
      - CAGE Code (text, required, maxLength 5) — maps to `cage_code`
        - Inline validation on blur using `validateCAGE()`
      - SAM Status (select: Active/Pending/Expired/Not Registered) — maps to `sam_status`
      - SAM Expiration Date (date input) — maps to `sam_expiration`

      **Business Classification section:**
      - Primary NAICS Code (text, required, maxLength 6 + title text field beside it)
        - Inline validation on blur using `validateNAICS()`
        - This captures the PRIMARY NAICS directly on company_profiles (the existing naics_codes table holds additional codes)
      - Business Size (select: Small / Other Than Small / Large) — maps to `business_size`
      - Socioeconomic Certifications (multi-select checkboxes) — maps to `socioeconomic_certs` array
        - Checkboxes for: 8(a), HUBZone, SDVOSB, VOSB, WOSB, EDWOSB, SDB, Minority-Owned
        - Styled as a grid of toggle-style checkboxes

      **Company Details section** (keep existing fields):
      - Year Founded, Employee Count, Annual Revenue, Fiscal Year End, Website
      - Headquarters Address (existing address group)

      **Primary Contacts section** (keep existing):
      - Proposal POC, Contracts POC, Authorized Signer

    - Save button at bottom right: "Save Corporate Identity"
    - On save: PUT to `/api/company/profile` via `fetchWithCompany()` with only this tab's fields
    - Run client-side validation before submit. If validation fails, show inline errors and do NOT submit.
    - Show success toast/banner on save, error banner on failure
    - Initialize form data from `initialData` prop on mount

    **page.tsx** — Rewrite the company profile page:
    - Keep 'use client', useCompany(), useFetchWithCompany() patterns
    - On mount: fetch company profile data via GET /api/company/profile
    - Render `Tier1TabLayout` with 3 tabs
    - Track `activeTab` state (default: 'corporate-identity')
    - Conditionally render the active tab component (CorporateIdentityTab for now; other tabs in Task 2)
    - Pass fetched profile data as `initialData` to the active tab
    - On save callback: re-fetch profile to update shared state
    - Keep existing loading spinner and "no company selected" patterns
    - Remove ALL old form content — this is a complete replacement
  </action>
  <verify>
    <automated>npx tsc --noEmit app/company/profile/page.tsx app/components/tier1/Tier1TabLayout.tsx app/components/tier1/CorporateIdentityTab.tsx 2>&1 | head -20</automated>
    <manual>Navigate to /company/profile. Verify 3-tab header appears. Corporate Identity tab shows all fields. UEI field shows validation on blur.</manual>
  </verify>
  <done>Tab layout renders 3 tabs. Corporate Identity tab displays all fields (identity, registration with UEI/CAGE validation, business classification with NAICS validation and socioeconomic certs, company details, contacts). Save button persists data via API. Switching tabs preserves unsaved Corporate Identity form state.</done>
</task>

<task type="auto">
  <name>Task 2: Create Vehicles & Certifications tab</name>
  <files>app/components/tier1/VehiclesCertificationsTab.tsx</files>
  <action>
    Create the Vehicles & Certifications tab component:
    - 'use client' component
    - Props: `companyId: string`, `initialProfileData: Partial<CompanyProfile>`, `onSaved: () => void`

    This tab manages TWO types of data:
    1. Profile-level fields (ISO/CMMI, DCAA) saved via PUT /api/company/profile
    2. Relational data (contract vehicles, certifications, facility clearance) saved via POST /api/company/certifications

    **Contract Vehicles section:**
    - Display existing vehicles as cards (fetched from GET /api/company/certifications on mount)
    - "Add Vehicle" button opens inline form:
      - Vehicle Name (text, required)
      - Contract Number (text)
      - Vehicle Type (select: GWAC, BPA, IDIQ, GSA Schedule, State/Local Vehicle)
      - Ordering Period End (date)
      - Ceiling Value (number)
    - Each card has Delete button (DELETE /api/company/certifications with type='vehicle' and id)
    - Save via POST /api/company/certifications with type='vehicle'

    **Quality & Compliance Certifications section:**
    - ISO/CMMI/ITIL Status — render as a grid of toggle switches (boolean toggles):
      - ISO 9001 Quality Management
      - ISO 27001 Information Security
      - ISO 20000 IT Service Management
      - CMMI-DEV Level (select: None, 1, 2, 3, 4, 5 — stored as number or null)
      - CMMI-SVC Level (select: None, 1, 2, 3, 4, 5)
      - ITIL Certified (boolean toggle)
    - These are stored as the `iso_cmmi_status` JSONB column on company_profiles

    **DCAA-Approved Systems section:**
    - Render as a grid of toggle switches (checkboxes):
      - Accounting System, Estimating System, Purchasing System, Timekeeping System, Compensation System, Billing System
    - Stored as `dcaa_approved_systems` JSONB column on company_profiles

    **Facility Clearance section:**
    - Has Facility Clearance (boolean toggle)
    - If yes, show:
      - Clearance Level (select: Confidential, Secret, Top Secret, TS/SCI)
      - Sponsoring Agency (text)
      - CAGE Code Cleared (text)
    - Save via POST /api/company/certifications with type='facility_clearance'

    **Socioeconomic Certifications section:**
    - Display existing certs from GET /api/company/certifications as cards
    - "Add Certification" button with form fields:
      - Certification Type (text or select with common types: 8(a), HUBZone, SDVOSB, WOSB, etc.)
      - Certifying Agency (text)
      - Certification Number (text)
      - Effective Date, Expiration Date (date inputs)

    **Save behavior:**
    - "Save Vehicles & Certifications" button at bottom
    - On save: PUT /api/company/profile with iso_cmmi_status and dcaa_approved_systems fields
    - Individual vehicle/certification/clearance items are saved inline when added (POST)
    - The Save button handles the profile-level JSONB fields

    Match existing codebase patterns: `fetchWithCompany()`, Tailwind cards, error/success banners.
  </action>
  <verify>
    <automated>npx tsc --noEmit app/components/tier1/VehiclesCertificationsTab.tsx 2>&1 | head -20</automated>
    <manual>Navigate to /company/profile, click Vehicles & Certifications tab. Add a contract vehicle. Toggle ISO 9001. Save. Reload and verify persistence.</manual>
  </verify>
  <done>Vehicles & Certifications tab displays contract vehicles (add/remove), ISO/CMMI/ITIL toggles, DCAA-approved system toggles, facility clearance fields, and socioeconomic certification cards. Profile-level JSONB fields save via PUT. Relational items save inline via POST. All data persists across page refresh.</done>
</task>

<task type="auto">
  <name>Task 3: Create Capabilities & Positioning tab</name>
  <files>app/components/tier1/CapabilitiesPositioningTab.tsx</files>
  <action>
    Create the Capabilities & Positioning tab component:
    - 'use client' component
    - Props: `companyId: string`, `initialProfileData: Partial<CompanyProfile>`, `onSaved: () => void`

    All fields in this tab are profile-level text fields saved via PUT /api/company/profile. This is the simplest tab — all plain textarea fields with word count enforcement.

    **Corporate Overview section:**
    - Corporate Overview (textarea, max 3000 chars) — maps to `corporate_overview`
      - Show word count indicator using `validateWordCount()` with min 50, max 500 words
      - Color-coded: gray when within range, yellow when approaching max (>80%), red when over max
      - Helper text: "Detailed corporate overview used in proposal executive summaries. Describe your company's mission, history, and core competencies."

    **Core Services section:**
    - Core Services Summary (textarea, max 2000 chars) — maps to `core_services_summary`
      - Word count: min 30, max 300 words
      - Helper text: "Summarize your primary service offerings. This feeds into proposal Technical Approach sections."

    **Enterprise Win Themes section:**
    - Win Themes (tag/list input) — maps to `enterprise_win_themes` TEXT array
      - "Add Win Theme" input + button pattern (like existing DBA names pattern)
      - Each theme is a short statement (< 100 chars), shown as removable tags
      - Helper text: "Reusable win themes that highlight your competitive advantages. Example: 'Proven transition methodology with zero disruption to operations'"
      - Minimum 3 win themes recommended (show yellow indicator if < 3)

    **Key Differentiators section:**
    - Key Differentiators Summary (textarea, max 2000 chars) — maps to `key_differentiators_summary`
      - Word count: min 30, max 300 words
      - Helper text: "Describe what sets you apart from competitors. This content is used across proposal sections to reinforce your value."

    **Standard Management Approach section:**
    - Standard Management Approach (textarea, max 3000 chars) — maps to `standard_management_approach`
      - Word count: min 50, max 500 words
      - Helper text: "Your standard approach to program management, quality assurance, and risk management. Used as the foundation for Management Volume content."

    **Elevator Pitch & Descriptions section** (migrated from old profile page):
    - Elevator Pitch (textarea, max 500 chars, required) — existing field
    - Full Description (textarea, max 2000 chars) — existing field
    - Mission Statement (textarea, max 300 chars) — existing field
    - Vision Statement (textarea, max 300 chars) — existing field
    - Core Values (tag input) — existing field

    **Save behavior:**
    - "Save Capabilities & Positioning" button at bottom right
    - Client-side validation: check all word count ranges before submit
    - If any field is over max: show inline error, prevent submit
    - If any field is under min: show yellow warning but allow save (recommendation, not blocking)
    - PUT /api/company/profile with all capability fields

    Use plain textarea elements (not rich text) per Claude's discretion — proposal generation consumes plain text and adds its own formatting. Rich text would add complexity without improving output quality.
  </action>
  <verify>
    <automated>npx tsc --noEmit app/components/tier1/CapabilitiesPositioningTab.tsx 2>&1 | head -20</automated>
    <manual>Navigate to /company/profile, click Capabilities & Positioning. Enter text in corporate overview. Verify word count indicator updates live. Save and verify persistence.</manual>
  </verify>
  <done>Capabilities & Positioning tab renders all text fields with live word count indicators. Enterprise win themes use tag input pattern. Word count over-max blocks save with inline error. Under-min shows yellow warning. All fields save via PUT to company profile API. Existing elevator pitch, descriptions, and core values migrated from old page layout.</done>
</task>

</tasks>

<verification>
1. Navigate to `/company/profile` — 3-tab interface renders with Corporate Identity as default tab
2. Corporate Identity tab: all fields from TIER1-01 present, UEI validation shows inline error on invalid input, SAM.gov status badge appears after valid UEI
3. Vehicles & Certifications tab: can add/remove contract vehicles, toggle ISO/CMMI/DCAA switches, set facility clearance
4. Capabilities & Positioning tab: word count indicators on all text fields, win themes tag input works
5. Per-tab save: changing tab does not lose unsaved work, each tab's Save button only saves its own data
6. All files pass TypeScript compilation
</verification>

<success_criteria>
- 3-tab layout replaces the old company profile page entirely
- All Blueprint section 4.1 Tier 1 fields are represented in the UI
- Inline validation on UEI, CAGE, NAICS fields with descriptive errors
- Word count enforcement on capability text fields
- Per-tab save with independent form state
- Unsaved changes preserved when switching tabs
</success_criteria>

<output>
After completion, create `.planning/phases/05-tier-1-enterprise-intake/05-02-SUMMARY.md`
</output>
