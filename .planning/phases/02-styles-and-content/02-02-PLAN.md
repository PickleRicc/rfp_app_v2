---
phase: 02-styles-and-content
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/generation/templates/technical-approach.ts
  - lib/generation/templates/management-approach.ts
autonomous: true

must_haves:
  truths:
    - "Every task area in Technical Approach includes a Deliverables subsection when applicable"
    - "Every approach section includes a Benefits callout box highlighting customer value"
    - "Deliverables subsection lists specific outputs per Framework Section 5.2"
    - "Benefits callouts use proper styling (bordered box, heading, content)"
  artifacts:
    - path: "lib/generation/templates/technical-approach.ts"
      provides: "Technical approach with Deliverables subsection and Benefits callout"
      contains: "generateDeliverables|Benefits|callout"
    - path: "lib/generation/templates/management-approach.ts"
      provides: "Management approach with Benefits callout boxes"
      contains: "Benefits|callout"
  key_links:
    - from: "lib/generation/templates/technical-approach.ts"
      to: "lib/generation/docx/styles.ts"
      via: "CalloutBox and CalloutBoxHeading styles (pre-existing in styles.ts lines 169-207)"
      pattern: "style.*Callout|CalloutBox"
      note: "CalloutBox and CalloutBoxHeading styles already exist in styles.ts per framework Section 4.2"
---

<objective>
Add Deliverables subsections and Benefits callout boxes to proposal content generation per Framework Sections 4.2 and 5.2.

Purpose: Complete the content structure required by the framework - deliverables show concrete outputs for each task area, benefits callouts highlight customer value propositions throughout the proposal.

Output: Updated technical-approach.ts with deliverables generation and benefits callouts, updated management-approach.ts with benefits callouts.

Note: CalloutBox and CalloutBoxHeading styles already exist in lib/generation/docx/styles.ts (lines 169-207). This plan uses those existing styles - no style creation required.
</objective>

<execution_context>
@C:\Users\eharr\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eharr\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-styles-and-content/02-01-SUMMARY.md
@Proposal_Response_Generation_Framework.md (Part 4.2: Call-Out Box System, Part 5.2: Technical Approach)
@lib/generation/templates/technical-approach.ts
@lib/generation/templates/management-approach.ts
@lib/generation/docx/styles.ts (contains CalloutBox and CalloutBoxHeading styles at lines 169-207)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Deliverables subsection to Technical Approach</name>
  <files>lib/generation/templates/technical-approach.ts</files>
  <action>
Per Framework Section 5.2 DELIVERABLES_SUBSECTION pattern, add deliverables generation to each task area in generateTechnicalApproach:

**Add helper function generateDeliverablesSubsection:**
```typescript
async function generateDeliverablesSubsection(
  taskArea: string,
  requirements: any[],
  companyData: any
): Promise<Paragraph[]>
```

This function should:
1. Create H3 heading "Deliverables" with numbering
2. Generate deliverables list using AI based on task area requirements
3. Use Bullet_1 style for deliverable items
4. Include reference to sample deliverables in appendix if applicable

**Modify generateTechnicalApproach:**
After the proof point paragraph for each task area, call generateDeliverablesSubsection and add the resulting paragraphs.

**AI prompt for deliverables should ask for:**
- 3-5 specific deliverables for the task area
- Each deliverable with: name, description, format (document, report, etc.)
- Response in JSON array format

Handle edge cases:
- If no relevant requirements, skip deliverables subsection
- If AI fails, generate generic deliverables placeholder
  </action>
  <verify>
Run `npx tsc --noEmit` - no type errors. Verify generateDeliverablesSubsection function exists.
  </verify>
  <done>
Technical approach generates Deliverables subsection for each task area with AI-generated deliverable items in bullet format.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Benefits callout boxes to approach sections</name>
  <files>lib/generation/templates/technical-approach.ts, lib/generation/templates/management-approach.ts</files>
  <action>
Per Framework Section 4.2 Call-Out Box System, add benefits callouts to approach sections.

**IMPORTANT: Use existing styles from lib/generation/docx/styles.ts:**
- `CalloutBoxHeading` (id: 'CalloutBoxHeading', lines 194-207) - bold, blue, 11pt
- `CalloutBox` (id: 'CalloutBox', lines 169-193) - left border, light blue shading

**Add helper function generateBenefitsCallout in technical-approach.ts:**
```typescript
function generateBenefitsCallout(
  benefitType: 'Key Benefit' | 'Proven Results' | 'Innovation Spotlight' | 'Risk Mitigation',
  content: string
): Paragraph[]
```

This function should:
1. Create heading paragraph with style: 'CalloutBoxHeading' and benefit type as text
2. Create content paragraph(s) with style: 'CalloutBox'
3. Return array of paragraphs that form the callout box

**Modify generateTechnicalApproach:**
After the methodology paragraph (before exhibit reference), add a Benefits callout:
- Use the `approach.benefit` content from AI generation
- Set benefitType to 'Key Benefit' (default) or vary based on content

**Modify management-approach.ts:**
Add benefits callouts to key sections:
- After Management Philosophy: "Key Benefit" callout
- After Organizational Structure: "Proven Results" callout (reference past performance)
- After Quality Control: "Risk Mitigation" callout

Limit to 1-2 callouts per major section as per framework guidelines.
  </action>
  <verify>
Run `npx tsc --noEmit` - no type errors. Verify CalloutBox and CalloutBoxHeading styles are used in generated content.
`grep -E "CalloutBox|CalloutBoxHeading" lib/generation/templates/technical-approach.ts` shows style usage.
  </verify>
  <done>
Benefits callout boxes appear in Technical Approach (per task area) and Management Approach (key sections) using existing CalloutBox and CalloutBoxHeading styles from styles.ts.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. `grep -E "generateDeliverables|Deliverables" lib/generation/templates/technical-approach.ts` shows deliverables function
3. `grep -E "generateBenefitsCallout|CalloutBox" lib/generation/templates/technical-approach.ts` shows callout usage
4. `grep -E "CalloutBox|Benefits" lib/generation/templates/management-approach.ts` shows callout usage in management approach
</verification>

<success_criteria>
- Deliverables subsection generates for each task area in Technical Approach
- Deliverables use proper H3 heading with numbering and bullet list
- Benefits callout boxes appear with proper styling (CalloutBoxHeading + CalloutBox)
- Callouts include appropriate benefit types (Key Benefit, Proven Results, etc.)
- Management Approach includes 2-3 strategically placed benefits callouts
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-styles-and-content/02-02-SUMMARY.md`
</output>
