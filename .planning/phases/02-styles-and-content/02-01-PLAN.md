---
phase: 02-styles-and-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/generation/docx/styles.ts
  - lib/generation/docx/numbering.ts
autonomous: true

must_haves:
  truths:
    - "Heading variants (Same Page, New Page, Unnumbered) can be applied to paragraphs"
    - "Emphasis character styles (Bold Intro, Intense Quote, Intense Reference) are available"
    - "3-level bullet list styles exist (Bullet_1, Bullet_2, Bullet_3)"
    - "4-level numbered list styles exist with correct numbering format"
    - "Table text style variants exist (heading, body, bold, centered in 10pt/11pt)"
    - "Table bullet styles exist for use within table cells"
  artifacts:
    - path: "lib/generation/docx/styles.ts"
      provides: "Complete paragraph and character styles per framework Part 3"
      contains: "Heading1SamePage|Heading1Unnumbered|Heading2NewPage|BoldIntro|IntenseQuote|Bullet3|TableHeading|NumberedList"
    - path: "lib/generation/docx/numbering.ts"
      provides: "Numbered list configuration and table bullet numbering"
      contains: "NUMBERED_LIST_NUMBERING|TABLE_BULLET_NUMBERING"
  key_links:
    - from: "lib/generation/docx/styles.ts"
      to: "lib/generation/docx/generator.ts"
      via: "PROPOSAL_STYLES import (already exists - verified at generator.ts line 14)"
      pattern: "import.*PROPOSAL_STYLES.*from.*styles"
      status: "pre-existing"
    - from: "lib/generation/docx/numbering.ts"
      to: "lib/generation/docx/generator.ts"
      via: "numbering config array"
      pattern: "numbering.*config.*\\["
---

<objective>
Extend the styling system with complete heading variants, emphasis styles, list styles, and table styles per the Proposal Response Generation Framework Part 3.

Purpose: Enable all professional document styling required by the framework - heading variants control page breaks, emphasis styles highlight key content, and complete list/table styles ensure consistent formatting throughout proposals.

Output: Updated styles.ts with all paragraph/character styles, updated numbering.ts with numbered list and table bullet configurations.

Note: CalloutBox and CalloutBoxHeading styles already exist in styles.ts (lines 169-207) per framework Section 4.2. No changes needed for callout box styling.
</objective>

<execution_context>
@C:\Users\eharr\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eharr\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Proposal_Response_Generation_Framework.md (Part 3: Writing Style System)
@lib/generation/docx/styles.ts
@lib/generation/docx/numbering.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add heading variants and emphasis styles to styles.ts</name>
  <files>lib/generation/docx/styles.ts</files>
  <action>
Extend PROPOSAL_STYLES with the following per Framework Part 3:

**Heading Variants (Part 3.1):**
- `Heading1SamePage` - H1 without page break (based on Heading1, remove keepNext/keepLines behavior that causes page break)
- `Heading1Unnumbered` - H1 without numbering (for Cover Letter, TOC, etc.)
- `Heading2NewPage` - H2 that forces page break (add pageBreakBefore: true)

**Emphasis Styles (Part 3.2 EMPHASIS_STYLES):**
- `BoldIntro` - character style: bold text for first sentence emphasis
- `IntenseQuote` - paragraph style: block quotes/key callouts with italic, left border, light shading
- `IntenseReference` - character style: italic with color for citations and references

All styles should:
- Use Arial font (matching existing styles)
- Use consistent color scheme (blues: 2563eb, 1e40af)
- Include proper spacing per framework guidelines
  </action>
  <verify>
Run `npx tsc --noEmit` - no type errors. Verify styles.ts exports the new style IDs.
  </verify>
  <done>
styles.ts contains Heading1SamePage, Heading1Unnumbered, Heading2NewPage paragraph styles and BoldIntro, IntenseQuote, IntenseReference character/paragraph styles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add list and table styles to styles.ts and numbering.ts</name>
  <files>lib/generation/docx/styles.ts, lib/generation/docx/numbering.ts</files>
  <action>
**In styles.ts - Add List Styles (Part 3.3):**
- `Bullet3` - third level bullet (indent 1080 twips, smaller bullet character)
- `TableBullet1_10pt` - bullet for table cells at 10pt
- `TableBullet1_11pt` - bullet for table cells at 11pt
- `TableBullet2_10pt` - second level table bullet

**In numbering.ts - Add Numbered List Config (Part 3.3):**
Create NUMBERED_LIST_NUMBERING constant:
- Level 0: "1.", "2.", "3." (DECIMAL)
- Level 1: "a.", "b.", "c." (LOWER_LETTER)
- Level 2: "i.", "ii.", "iii." (LOWER_ROMAN)
- Level 3: "1)", "2)", "3)" (DECIMAL with parenthesis)

Create TABLE_BULLET_NUMBERING constant:
- Single level bullet for table cells

Export `getNumberedListNumbering(level: number)` function similar to getHeadingNumbering.

**In styles.ts - Add Table Styles (Part 3.4):**
- `TableHeading10pt` - centered, bold, white text
- `TableHeading10ptLeft` - left-aligned header
- `TableHeading10ptRight` - right-aligned header
- `TableHeading11pt` - 11pt variants
- `TableText10pt` - standard cell text
- `TableText11pt` - larger cell text
- `TableTextBold10pt` - emphasis cells
- `TableTextCenter10pt` - centered data cells
  </action>
  <verify>
Run `npx tsc --noEmit` - no type errors. Verify numbering.ts exports NUMBERED_LIST_NUMBERING, TABLE_BULLET_NUMBERING, and getNumberedListNumbering function.
  </verify>
  <done>
Complete list styles (Bullet3, TableBullet variants) and table styles (TableHeading/Text variants) in styles.ts. Numbered list and table bullet numbering configs in numbering.ts with helper function.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. `grep -E "Heading1SamePage|Heading1Unnumbered|Heading2NewPage" lib/generation/docx/styles.ts` shows all heading variants
3. `grep -E "BoldIntro|IntenseQuote|IntenseReference" lib/generation/docx/styles.ts` shows emphasis styles
4. `grep -E "Bullet3|TableBullet" lib/generation/docx/styles.ts` shows list styles
5. `grep -E "TableHeading|TableText" lib/generation/docx/styles.ts` shows table styles
6. `grep -E "NUMBERED_LIST_NUMBERING|getNumberedListNumbering" lib/generation/docx/numbering.ts` shows numbering exports
</verification>

<success_criteria>
- All heading variant styles defined and available for use
- All emphasis styles (character and paragraph) defined
- Complete 3-level bullet system with table variants
- Complete 4-level numbered list system with numbering config
- Complete table text style variants (10pt/11pt, alignment variants)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-styles-and-content/02-01-SUMMARY.md`
</output>
