---
phase: 02-styles-and-content
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/generation/docx/generator.ts
  - lib/generation/docx/styles.ts
  - lib/generation/templates/technical-approach.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Lists support 3-level bullets and numbered lists with 4 levels matching framework styles"
  artifacts:
    - path: "lib/generation/docx/generator.ts"
      provides: "Numbering config array with all list types"
      contains: "NUMBERED_LIST_NUMBERING"
    - path: "lib/generation/docx/styles.ts"
      provides: "Consistent bullet style IDs"
      contains: "id: 'Bullet1'"
    - path: "lib/generation/templates/technical-approach.ts"
      provides: "Correct bullet style reference"
      contains: "style: 'Bullet1'"
  key_links:
    - from: "lib/generation/docx/numbering.ts"
      to: "lib/generation/docx/generator.ts"
      via: "import and config array"
      pattern: "NUMBERED_LIST_NUMBERING|TABLE_BULLET_NUMBERING"
---

<objective>
Wire numbering configurations into document generator and fix bullet style ID mismatch

Purpose: Close verification gap - NUMBERED_LIST_NUMBERING and TABLE_BULLET_NUMBERING are defined in numbering.ts but never added to the generator's numbering config. Additionally, technical-approach.ts uses 'Bullet_1' but styles.ts defines 'Bullet1' (no underscore), causing formatting failures.

Output: Generator properly configured with all numbering systems; consistent style IDs throughout codebase
</objective>

<execution_context>
@C:\Users\eharr\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eharr\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-styles-and-content/02-styles-and-content-VERIFICATION.md
@.planning/phases/02-styles-and-content/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add list numbering configs to generator.ts</name>
  <files>lib/generation/docx/generator.ts</files>
  <action>
    1. Import NUMBERED_LIST_NUMBERING and TABLE_BULLET_NUMBERING from './numbering' (add to existing import)

    2. Update the numbering.config array (currently line 54, only has HEADING_NUMBERING) to include all three configs:
    ```typescript
    numbering: {
      config: [HEADING_NUMBERING, NUMBERED_LIST_NUMBERING, TABLE_BULLET_NUMBERING],
    },
    ```

    This enables the document to use numbered lists and table bullets that are already defined in numbering.ts but were never wired into the generator.
  </action>
  <verify>
    - grep -n "NUMBERED_LIST_NUMBERING" lib/generation/docx/generator.ts
    - grep -n "TABLE_BULLET_NUMBERING" lib/generation/docx/generator.ts
    - Both should appear in imports AND in the config array
    - npm run build completes without errors
  </verify>
  <done>Generator's numbering config includes HEADING_NUMBERING, NUMBERED_LIST_NUMBERING, and TABLE_BULLET_NUMBERING</done>
</task>

<task type="auto">
  <name>Task 2: Fix bullet style ID mismatch</name>
  <files>lib/generation/templates/technical-approach.ts</files>
  <action>
    1. Find line 398 (approximately) where style: 'Bullet_1' is used in the deliverables list

    2. Change 'Bullet_1' to 'Bullet1' to match the style ID defined in styles.ts (line 210):
    ```typescript
    // Before:
    style: 'Bullet_1',

    // After:
    style: 'Bullet1',
    ```

    3. Search for any other occurrences of 'Bullet_1', 'Bullet_2', or 'Bullet_3' in templates and fix to 'Bullet1', 'Bullet2', 'Bullet3' respectively.

    WHY: styles.ts defines `id: 'Bullet1'` (no underscore) but technical-approach.ts references `style: 'Bullet_1'` (with underscore). This mismatch causes the style to not be applied.
  </action>
  <verify>
    - grep -r "Bullet_" lib/generation/ should return NO matches
    - grep -r "style: 'Bullet" lib/generation/ should show Bullet1, Bullet2, Bullet3 (no underscores)
    - npm run build completes without errors
  </verify>
  <done>All bullet style references use consistent IDs matching styles.ts definitions (Bullet1, Bullet2, Bullet3)</done>
</task>

</tasks>

<verification>
After completing both tasks:
1. `npm run build` passes
2. `grep -c "NUMBERED_LIST_NUMBERING" lib/generation/docx/generator.ts` returns 2 (import + config)
3. `grep -c "TABLE_BULLET_NUMBERING" lib/generation/docx/generator.ts` returns 2 (import + config)
4. `grep -r "Bullet_" lib/generation/` returns no matches
5. The numbering config array contains all three numbering systems
</verification>

<success_criteria>
- Generator can use numbered lists (getNumberedListNumbering()) in templates
- Generator can use table bullets in table cells
- Bullet styles in technical-approach.ts actually apply (no more mismatch)
- Document generation succeeds with all list types available
</success_criteria>

<output>
After completion, create `.planning/phases/02-styles-and-content/02-04-SUMMARY.md`
</output>
