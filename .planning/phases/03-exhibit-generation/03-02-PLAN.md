---
phase: 03-exhibit-generation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/generation/exhibits/org-chart.ts
  - lib/generation/exhibits/process-diagram.ts
  - lib/generation/exhibits/timeline.ts
autonomous: true

must_haves:
  truths:
    - "Org chart renders actual personnel data with names, titles, and reporting lines"
    - "Process diagram renders workflow steps as flowchart"
    - "Timeline renders Gantt chart with phases and durations"
    - "All diagrams use company branding colors"
  artifacts:
    - path: "lib/generation/exhibits/org-chart.ts"
      provides: "Org chart PNG generation from personnel data"
      exports: ["generateOrgChart"]
    - path: "lib/generation/exhibits/process-diagram.ts"
      provides: "Process flowchart PNG generation"
      exports: ["generateProcessDiagram"]
    - path: "lib/generation/exhibits/timeline.ts"
      provides: "Gantt chart PNG generation"
      exports: ["generateGanttChart"]
  key_links:
    - from: "lib/generation/exhibits/org-chart.ts"
      to: "lib/generation/exhibits/render.ts"
      via: "import renderMermaidToPng"
      pattern: "renderMermaidToPng"
    - from: "lib/generation/exhibits/org-chart.ts"
      to: "lib/generation/exhibits/branding.ts"
      via: "import getDiagramTheme"
      pattern: "getDiagramTheme"
---

<objective>
Refactor diagram generators to produce actual PNG images

Purpose: Replace stub implementations with working Mermaid-based diagram generation using the shared rendering infrastructure from Plan 01.

Output: Three working diagram generators (org-chart, process-diagram, timeline) that produce PNG files
</objective>

<execution_context>
@C:\Users\eharr\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\eharr\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-exhibit-generation/03-CONTEXT.md
@.planning/phases/03-exhibit-generation/03-01-SUMMARY.md

# Infrastructure from Plan 01
@lib/generation/exhibits/render.ts
@lib/generation/exhibits/branding.ts

# Existing implementations to refactor
@lib/generation/exhibits/org-chart.ts
@lib/generation/exhibits/process-diagram.ts
@lib/generation/exhibits/timeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor org-chart.ts to use shared renderer with branding</name>
  <files>lib/generation/exhibits/org-chart.ts</files>
  <action>
Refactor generateOrgChart to:

1. Import and use renderMermaidToPng from ./render.ts
2. Import and use getDiagramTheme from ./branding.ts
3. Remove the local renderMermaidDiagram function (use shared one)
4. Add theme directive to Mermaid code for consistent styling

Update generateMermaidOrgChart to prepend theme:
```typescript
const theme = getDiagramTheme();
return `${theme}
graph TD
    ${nodes.join('\n    ')}
    ${edges.join('\n    ')}`;
```

Update generateOrgChart to:
- Call renderMermaidToPng(mermaid, 'org-chart')
- Return the path to the generated PNG
- Handle empty personnel gracefully (return empty string)

Add customer/COR interface box per CONTEXT.md:
- If personnel data available, add a "Government Customer" node at top
- Connect Program Manager to Government Customer with dashed line
- Use different styling for customer node (light gray fill)

Box content per CONTEXT.md: Name + Title + Clearance (if available)
Update node generation to include clearance:
```typescript
`${nodeId}["${role.role_title}<br/>${person.name}${person.clearance_level ? '<br/>' + person.clearance_level : ''}"]`
```
  </action>
  <verify>
TypeScript compiles: `cd C:\Users\eharr\Dev_2026\rfp_app_v2 && npx tsc --noEmit`
  </verify>
  <done>
org-chart.ts uses shared renderer, includes branding, shows clearance levels, connects to customer
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor process-diagram.ts to use shared renderer with branding</name>
  <files>lib/generation/exhibits/process-diagram.ts</files>
  <action>
Refactor generateProcessDiagram to:

1. Import and use renderMermaidToPng from ./render.ts
2. Import and use getDiagramTheme from ./branding.ts
3. Remove the local renderMermaidDiagram function (use shared one)

Update generateMermaidFlowchart to:
- Prepend theme directive
- Use standard flowchart shapes per CONTEXT.md:
  - Rectangles for steps (default)
  - Stadium shapes for start/end: `([text])`
  - Decision diamonds: `{text}` (if step contains "decide" or "if")
- Keep diagrams high-level and readable (truncate step text to 40 chars)

Update function signature to accept optional decision points:
```typescript
export async function generateProcessDiagram(
  processName: string,
  steps: string[],
  decisionPoints?: { afterStep: number; yesLabel?: string; noLabel?: string }[]
): Promise<string>
```

For STANDARD_PROCESSES, update to use the new renderer.
  </action>
  <verify>
TypeScript compiles: `cd C:\Users\eharr\Dev_2026\rfp_app_v2 && npx tsc --noEmit`
  </verify>
  <done>
process-diagram.ts uses shared renderer with branding, supports decision points
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor timeline.ts to use shared renderer with branding</name>
  <files>lib/generation/exhibits/timeline.ts</files>
  <action>
Refactor generateGanttChart to:

1. Import and use renderMermaidToPng from ./render.ts
2. Import and use getDiagramTheme from ./branding.ts
3. Remove the local renderMermaidDiagram function (use shared one)

Update generateMermaidGantt to:
- Prepend theme directive
- Use wider output (1200px) for Gantt readability
- Format dates clearly: axisFormat %b %d (%b for month name)

Add support for milestone markers per CONTEXT.md:
```typescript
export interface TimelinePhase {
  name: string;
  duration: number;  // days
  activities?: string[];
  milestone?: string;  // Optional milestone at end of phase
}
```

If phase has milestone, add milestone marker:
```
milestone ${phase.milestone}: milestone, ${endDate}, 0d
```

Fix generateDefaultTimeline to actually return rendered image (currently returns empty string).
Call renderMermaidToPng with default 4-phase transition timeline.

Update generateMilestoneTimeline to also use renderer.
  </action>
  <verify>
TypeScript compiles: `cd C:\Users\eharr\Dev_2026\rfp_app_v2 && npx tsc --noEmit`
  </verify>
  <done>
timeline.ts uses shared renderer with branding, supports milestones, default timeline works
  </done>
</task>

</tasks>

<verification>
- [ ] All three diagram files use renderMermaidToPng from render.ts
- [ ] All three diagram files use getDiagramTheme from branding.ts
- [ ] No local renderMermaidDiagram functions remain
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
- All diagram generators use shared infrastructure
- Org chart shows personnel with clearances and customer interface
- Process diagrams support decision points
- Timelines support milestones
- All diagrams use consistent branding colors
</success_criteria>

<output>
After completion, create `.planning/phases/03-exhibit-generation/03-02-SUMMARY.md`
</output>
