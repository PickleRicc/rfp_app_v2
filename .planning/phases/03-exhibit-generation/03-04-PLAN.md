---
phase: 03-exhibit-generation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/generation/templates/management-approach.ts
  - lib/generation/templates/transition-plan.ts
  - lib/generation/templates/technical-approach.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All exhibit references in narrative text use bold+italic formatting per Framework Part 4.1"
    - "Narrative text introduces each exhibit BEFORE the exhibit image appears"
    - "Cross-references follow pattern: 'as shown in ***Exhibit X***' or '***Exhibit X*** shows...'"
  artifacts:
    - path: "lib/generation/templates/management-approach.ts"
      provides: "Org chart exhibit cross-reference in narrative"
      contains: "bold: true, italics: true"
    - path: "lib/generation/templates/transition-plan.ts"
      provides: "Timeline exhibit cross-reference in narrative"
      contains: "bold: true, italics: true"
    - path: "lib/generation/templates/technical-approach.ts"
      provides: "Process diagram exhibit cross-reference in narrative"
      contains: "bold: true, italics: true"
  key_links:
    - from: "Narrative paragraph"
      to: "Exhibit image"
      via: "Cross-reference with exhibit number"
      pattern: "Exhibit \\$\\{exhibitNum\\}.*bold.*italics"
---

<objective>
Verify and ensure exhibit cross-references exist in narrative text before each exhibit image.

Purpose: Framework Part 4.1 requires exhibits to be referenced in narrative text using bold+italic formatting ("as shown in ***Exhibit X***"). Verification found no cross-references matching this pattern. This plan verifies existing code and adds any missing cross-references.

Output: All three template files have proper exhibit cross-references in narrative text before exhibit images.
</objective>

<execution_context>
@C:\Users\eharr\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\eharr\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-exhibit-generation/03-exhibit-generation-VERIFICATION.md

# Prior work establishing exhibit embedding
@.planning/phases/03-exhibit-generation/03-03-SUMMARY.md

# Files to modify
@lib/generation/templates/management-approach.ts
@lib/generation/templates/transition-plan.ts
@lib/generation/templates/technical-approach.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enhance exhibit cross-references in all templates</name>
  <files>
    lib/generation/templates/management-approach.ts
    lib/generation/templates/transition-plan.ts
    lib/generation/templates/technical-approach.ts
  </files>
  <action>
For each template file, verify exhibit cross-references exist with proper formatting. If missing or incorrectly formatted, add/fix them.

**Framework Part 4.1 Requirements:**
- Exhibit references must use bold+italic: `bold: true, italics: true` on TextRun
- Reference should appear in narrative BEFORE the exhibit image
- Format: "as shown in ***Exhibit X***" or "***Exhibit X*** shows/provides..."

**management-approach.ts (org chart):**
Check lines ~92-114 for the org chart reference. Should have:
```typescript
new TextRun({
  text: `Exhibit ${exhibitNum}`,
  bold: true,
  italics: true,
  size: 22,
}),
```
If missing, add narrative paragraph before org chart image (around line 128) with text like:
"Our organizational structure, shown in ***Exhibit X***, establishes clear reporting relationships..."

**transition-plan.ts (timeline):**
Check lines ~44-61 for the timeline reference. Should have bold+italic on "Exhibit N".
If missing, add narrative paragraph before timeline image (around line 73) with text like:
"***Exhibit Y*** illustrates our transition timeline with four distinct phases..."

**technical-approach.ts (process diagram):**
Check lines ~157-181 for the process diagram reference. Should have bold+italic on "Exhibit N".
If missing, add narrative paragraph before each process diagram image (around line 192) with text like:
"As shown in ***Exhibit Z***, our [task area] methodology ensures..."

**Important:**
- The `bold: true, italics: true` properties MUST be on the same TextRun that contains the exhibit reference text
- Do NOT use separate TextRun elements for bold vs italic
- Verify the reference appears BEFORE the ImageRun in the paragraph array
  </action>
  <verify>
Run grep to confirm bold+italic exhibit references exist:
```bash
grep -n "bold: true" lib/generation/templates/management-approach.ts | grep -A1 "italics: true" | grep -i "exhibit"
grep -n "bold: true" lib/generation/templates/transition-plan.ts | grep -A1 "italics: true" | grep -i "exhibit"
grep -n "bold: true" lib/generation/templates/technical-approach.ts | grep -A1 "italics: true" | grep -i "exhibit"
```

Alternatively, search for the pattern directly:
```bash
grep -n "Exhibit.*bold.*true.*italics.*true" lib/generation/templates/*.ts
```

Each file should have at least one match showing bold+italic formatting on exhibit references.
  </verify>
  <done>
All three template files have exhibit cross-references with:
- bold: true AND italics: true on the TextRun containing "Exhibit N"
- Reference appears in narrative paragraph BEFORE the ImageRun
- Text follows Framework Part 4.1 pattern
  </done>
</task>

</tasks>

<verification>
After task completion, verify:

1. **Pattern search finds references in all files:**
```bash
for f in lib/generation/templates/management-approach.ts lib/generation/templates/transition-plan.ts lib/generation/templates/technical-approach.ts; do
  echo "=== $f ==="
  grep -n "bold: true" "$f" | head -5
done
```

2. **Build succeeds:**
```bash
npm run build
```

3. **TypeScript compiles without errors:**
```bash
npx tsc --noEmit
```
</verification>

<success_criteria>
- All three template files have exhibit cross-references with bold: true AND italics: true
- References appear in narrative text BEFORE exhibit images (not after)
- Build passes without errors
- Gap "All exhibit references in text correspond to actual generated exhibits" is now closeable
</success_criteria>

<output>
After completion, create `.planning/phases/03-exhibit-generation/03-04-SUMMARY.md`
</output>
